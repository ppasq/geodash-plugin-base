---
id: tooltip
type: object
label: Tooltip
placeholder: "tooltip"
required: false
description: "Bootstrap tooltips.  See [http://v4-alpha.getbootstrap.com/components/tooltips/](http://v4-alpha.getbootstrap.com/components/tooltips/)."
schema:
  verbose_singular: Tooltip
  fields:
    - id: content
      type: template
      label: Tooltip / Content
      placeholder: auto
      required: true
      description: "The content of the tooltip."
    - id: container
      type: text
      label: Tooltip / Container
      placeholder: auto
      required: true
      description: "The container of the tooltip."
      examples:
        - "body"
        - "#geodash-map-navbars"
    - id: placement
      type: text
      label: Tooltip / Placement
      placeholder: auto
      required: true
      description: "The placement of the tooltip."
      options: [top, left, bottom, right]
